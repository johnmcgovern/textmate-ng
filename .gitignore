# /build/    = xcodebuild output; /ide/gen/ = generated farm + specs.json + logs
# TextMate.xcodeproj is regenerated by ide/seed_xcodeproj.rb during the seeding
# phase; it will be committed once we cut over to "maintain natively".
/build/
/ide/gen/
/TextMate.xcodeproj/
.DS_Store

# Local planning notes, deliberately unpublished (still on disk, just untracked)
/NOTARIZATION_HANDOFF.md
/ide/local/
