// Hand-written ObjC declaration of the Swift OakLinkedSearchField (ui/SearchField.swift),
// for its one caller OakChooser.mm, still ObjC++. The private cell class stays internal to
// the Swift file; the cell registration is pinned by t_searchfield.mm (rule 18). Disappears
// once OakChooser is Swift too.
@interface OakLinkedSearchField : NSSearchField
@end
